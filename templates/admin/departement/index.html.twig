{% extends 'base.html.twig' %}

{% block title %} Liste des départements{% endblock %}

{% block stylesheets %}

{% endblock %}

        {% block content %}
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="d-flex flex-wrap align-items-center justify-content-between mb-4">
                            <div>
                                <h4 class="mb-3">Liste des départements </h4>

                            </div>
                            <a href="{{ path('add_departement') }}" class="btn btn-primary add-list"><i class="las la-plus mr-3"></i>Ajouter un département  </a>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <div class="table-responsive rounded mb-3">
                            {{ include('default/FlashBag/flashBag.html.twig') }}

                            <table class="data-table table mb-0 tbl-server-info">
                                <thead class="bg-white text-uppercase">
                                <tr class="ligth ligth-data">

                                    <th>Code</th>
                                    <th>Libelle</th>
                                    <th>Actions</th>
                                </tr>
                                </thead>
                                <tbody class="ligth-body">
                                {%  for d in departemets %}
                                <tr>
                                    <td>{{ d.codeDeppart }}</td>
                                    <td>{{ d.libelle }}</td>
                                    <td>
                                        <div class="d-flex align-items-center list-action">
                                            <a class="badge bg-success mr-2" data-toggle="tooltip" data-placement="top" title="" data-original-title="Modifier" href="{{path('edit_departement', {id: d.id})}}"><i class="ri-pencil-line mr-0"></i></a>
                                        </div>
                                    </td>

                                </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <!-- Page end  -->
            </div>


        {% endblock %}


{% block javascripts %}

{% endblock %}