{% extends 'base.html.twig' %}

{% block title %} Liste des clients {% endblock %}

{% block stylesheets %}

{% endblock %}

        {% block content %}
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="d-flex flex-wrap align-items-center justify-content-between mb-4">
                            <div>
                                <h4 class="mb-3">Liste Clients </h4>

                            </div>
                            <a href="{{ path('add_client') }}" class="btn btn-primary add-list"><i class="las la-plus mr-3"></i>Ajouter client  </a>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <div class="table-responsive rounded mb-3">
                            {{ include('default/FlashBag/flashBag.html.twig') }}

                            <table class="data-table table mb-0 tbl-server-info">
                                <thead class="bg-white text-uppercase">
                                <tr class="ligth ligth-data">

{#                                    <th>Date</th>#}
                                    <th>Code client </th>
                                    <th>Code TVA</th>
                                    <th>Nom</th>
                                    <th>Prenom</th>
                                    <th>Adresse</th>
                                    <th>Telephone</th>
                                    <th>Email</th>
                                    <th>Actions</th>
                                </tr>
                                </thead>
                                <tbody class="ligth-body">
                                {%  for cl in clients %}
                                <tr>
{#                                    <td>{{ cl.createdAt|date('d-m-Y') }}</td>#}
                                    <td>{{ cl.code }}</td>
                                    <td>{{ cl.codeTVA }}</td>
                                    <td>{{ cl.nom }}</td>
                                    <td>{{ cl.prenom }}</td>
                                    <td>{{ cl.adresse }}</td>
                                    <td>{{ cl.telephone }}</td>
                                    <td>{{ cl.email }}</td>
                                    <td>
                                        <div class="d-flex align-items-center list-action">
                                            <a class="badge bg-success mr-2" data-toggle="tooltip" data-placement="top" title="" data-original-title="Edit" href="{{path('edit_client', {id: cl.id})}}"><i class="ri-pencil-line mr-0"></i></a>
                                            <a class="badge bg-warning mr-2" data-toggle="tooltip" data-placement="top" title="" data-original-title="Delete" href="{{path('delete_client', {id: cl.id})}}"><i class="ri-delete-bin-line mr-0"></i></a>
                                        </div>
                                    </td>

                                </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <!-- Page end  -->
            </div>


        {% endblock %}


{% block javascripts %}

{% endblock %}